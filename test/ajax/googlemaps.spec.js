import axios from 'axios';


describe('Google Maps AJAX requests', () => {
  describe('Geo Location', () => {
    it('makes calls to the google Geo Location server', () => {});
    it('uses the secret api key in the call', () => {});
    it('pulls the latitude for based on cell tower info', () => {});
    it('pulls the longitude for based on cell tower info', () => {});
  }); // end describe('Geo Location')
  describe('Geo Coding', () => {
    it('makes calls to the google Geo Coding server', () => {});
    it('uses the secret api key in the call', () => {});
    it('pulls the latitude for a given address', () => {});
    it('pulls the longitude for a given address', () => {});
  }); // end describe('Geo Coding')
}); // end describe('DarkSky AJAX requests')
